import 'package:flutter/material.dart';

class YourRequirements extends StatelessWidget {
  static const routeName = '/yourRequirements';
  const YourRequirements({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
