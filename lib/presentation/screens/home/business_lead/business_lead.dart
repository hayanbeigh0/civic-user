import 'package:flutter/material.dart';

class BusinessLead extends StatelessWidget {
  static const routeName = '/businessLead';
  const BusinessLead({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
